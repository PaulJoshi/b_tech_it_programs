# Find the number of occurrences of a given substring in a string

st = input("\nEnter a string: ")
substring = input("Enter a substring: ")

print(f"The substring appears {st.count(substring)} times in the string\n")