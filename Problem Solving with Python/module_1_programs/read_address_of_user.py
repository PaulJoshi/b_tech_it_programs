#program to read address of user through interactive mode. Display the result by breaking it in multiple lines

#Write a program to read the address of a user through interactive mode. Display the result by breaking it in multiple lines

city = input("Enter city")
state = input("Enter state")
country = input("Enter country")

print("Address is \n" + city + "\n" + state + "\n" + country)